# Faz 18 final kabul kanıtı — 11 Ağustos 2026

## Sonuç

Faz 17'de araştırılan 15 adayın tamamı hesaplandı:

- İlk batch: MobileAction otomatik; SİMSOFT, Netaş ve Bilişim AŞ manuel.
- İkinci batch: Binalyze/Ashby ve Insider One/Lever otomatik.
- Üçüncü batch: Etiya ve Udemy otomatik; OBSS, T2 Software ve TaleWorlds
  manuel; LOTEC araştırılıyor.
- Son batch: Ankara Bilgi Teknolojileri ve Peaksoft Consulting manuel.
- Alictus, güncel SciPlay Games Turkey kimliğini duplicate etmemek için
  `not_eligible` olarak production dışında.

Davranış revision'ları `43bd2b4` (ikinci batch), `a52cee9` (üçüncü batch) ve
tam birleşik/kalite revision'ı `ec9f4ac` olarak upstream `main` üzerindedir.
Final katalog 41 şirket, 43 kaynak, 16 otomatik, 16 manuel ve 11 araştırılıyor
kaynaktır. On beş benzersiz şirket otomatik izlenir; genel otomatik kapsam
`%59,26`, birincil `%60`, ikincil `%58,82` ve Faz 18 normal ikincil kohortu
`%90`'dır.

## Test-first ve davranış kanıtı

- Ashby testleri önce eksik constructor/registry nedeniyle; Binalyze ve Insider
  config kabulü eksik şirket nedeniyle kırmızı oldu.
- Etiya tablo fixture'ı önce başlığı “View & Apply” çıkardığı, Greenhouse fixture'ı
  takip query/fragment'ını koruduğu ve üçüncü batch şirketleri eksik olduğu için
  kırmızı oldu.
- Son katalog kabulü Ankara Bilgi Teknolojileri eksikliği nedeniyle kırmızı
  oldu. Uygulama sonrasında bütün hedef ve backend testleri geçti.
- Binalyze/Insider çift taraması dört görünür genel tam zamanlı ilanı dedup etti
  ve sıfır öğrenci push'ı üretti.
- Etiya/Udemy çift taraması dört görünür ilanı dedup etti; yalnız backend odaklı
  güçlü staj fixture'ı bir ikincil push üretti.
- Başvuru sayfaları, LinkedIn, mailto/form hedefleri fetch edilmedi; CAPTCHA,
  HTTP 403/500 veya bot challenge aşılmadı. Normal testler canlı site veya
  ücretli model API'si çağırmadı.

## Tam kalite — exact `ec9f4ac`

- İzlenen Go dosyalarında `gofmt -l`: çıktı yok; `git diff --check` ve production
  kaynak JSON parse kontrolü: PASS.
- Go 1.26.5 container'ında `go test ./... -count=1`, CGO toolchain ile
  `go test -race ./... -count=1` ve `go vet ./...`: PASS.
- API, backup, restorecheck, dbinspect ve vapidkey build'leri: PASS.
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 güvenlik açığı.
- Node 24.18.0 ile temiz `npm ci`, 6 dosya/21 test, ayrı typecheck, production
  build ve `npm audit --audit-level=high`: PASS, 0 açık.
- Production contract, exact-commit workflow, deploy runtime ve preflight izin
  sözleşmeleri: PASS. Snap Docker sistem `/tmp` bind'ini görmediği için fixture
  repository içindeki tek kullanımlık ve test sonunda kaldırılan TMPDIR'de
  çalıştırıldı.
- Gerçek backend/frontend Docker image build'leri: PASS.
- Production'dan ayrı `internship-tracker-phase18-final-qa` Compose projesi
  `up --build --wait` sonunda API ve web healthy oldu.
- API `/health`, API/web `/ready`, proxied `/api/v1/coverage` ve build edilmiş
  CSS smoke: PASS. CSS bundle'ında `min-width:0`, `overflow-wrap:anywhere` ve
  `white-space:normal` doğrulandı.
- QA `dbinspect`: 41 şirket, 43 kaynak ve 14 migration. Container ve network
  kaldırıldı; `internship-tracker-phase18-final-qa_tracker_data` ile
  `internship-tracker-phase18-final-qa_tracker_backups` volume'ları korundu.

## Production kapısı

Kalan Faz 18 batch'leri production'a deploy edilmedi. Production API/web hâlâ
ilk batch'in doğrulanmış `eec2f63949f9f0de77830a0ebd62577631bce1c7`
revision'ındadır. Çalışan container yeni config'i restart olmadan yeniden
okumaz; ancak bind-mount edilen config artık `ashby_board` içerdiği için eski
binary deploy öncesi yeniden başlarsa config doğrulamasında durabilir.

AGENTS.md gereği `ec9f4ac` production rollout'u ayrı kullanıcı onayı bekler.
Onay sonrasında pre-deploy snapshot ve iki release ile restorecheck yapılmalı;
exact revision deploy edilmeli; health/readiness, Cloudflare dış smoke, final
41/43 coverage ve kalıcı `internship_tracker_data` kimliği raporlanmalıdır.
