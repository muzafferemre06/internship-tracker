# Faz 16 kabul kanıtı — 11 Ağustos 2026

## Doğrulanan sözleşme

- Kullanıcı onaylı on dört Faz 16 şirketi kanonik kimlikleriyle production
  config'inden gerçek geçici SQLite'a kaydedildi. `İnova` → `İnnova`,
  `ÜşüSebit` → `Sebit` ve `MechSoft AI` → `MechSoft` alias'ları çözüldü;
  AI Studio `aistudio.com.tr`, Bilishim `bilishim.ai` kimliğiyle izlendi.
- Evreka, MechSoft ve Layermark'ın açık, robots-uyumlu kariyer sayfaları
  fixture-first deterministik `career_links` adapter'ıyla işlendi. Hesap,
  oturum veya sosyal ağ yolu gerektiren kaynaklara otomatik istek yapılmadı.
- Mevcut ikincil STM dahil kapsama 15 şirket ve 15 kaynak olarak raporlandı:
  4 otomatik, 5 manuel, 6 araştırılıyor; manuel kaynaklar çıkarıldığında
  otomatik kapsama %40'tır.
- Coverage API/PWA genel toplamı ve `primary`/`secondary` özetlerini ayrı
  sunar. İkincil fırsat yalnız odak alanı eşleşmesi, en az `0.7` güven ve
  yüksek kaynak güvenini birlikte sağladığında versionlanmış ikincil event ile
  push üretir.
- Uçtan uca test iki taramada production kataloğunu ve production-shape
  fixture'ları işler: iki listing görünür kalır, zayıf aday push üretmez, güçlü
  aday tek push üretir ve ikinci tarama yeni listing/bildirim üretmez.
- Normal kabul tamamen fixture/fake transport ve geçici SQLite kullanır; canlı
  kariyer sitesi veya ücretli model API çağrısı yapmaz.

## Komut ve sonuç

```text
go test ./internal/acceptance -run TestPhase16 -count=1 -v
=== RUN   TestPhase16SecondaryCatalogCoverageAndStrongMatchNotificationEndToEnd
--- PASS: TestPhase16SecondaryCatalogCoverageAndStrongMatchNotificationEndToEnd
PASS
```

## Tam kalite kapısı

Davranış revision'ı: `a069c16012d86eadc112b0d8c02f95390950384a`.

- `go test ./... -count=1`: PASS
- `go test -race ./... -count=1`: PASS
- `go vet ./...`: PASS
- API, backup, restorecheck, dbinspect ve vapidkey build'leri: PASS
- İzlenen Go dosyalarında `gofmt -l`: çıktı yok; `git diff --check`: PASS
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 açık
- Temiz `npm ci`, frontend testleri (5 dosya/18 test), typecheck ve production
  build: PASS
- `npm audit --audit-level=high`: 0 açık
- Production contract, deploy runtime, exact-commit workflow ve preflight izin
  testleri: PASS. Production contract, Docker'ın `/tmp` namespace ayrımı
  nedeniyle test değiştirilmeden workspace altındaki mutlak geçici dizinle
  çalıştırıldı.
- Backend ve frontend gerçek Docker image build'leri: PASS
- Production'dan farklı proje adıyla gerçek development Compose
  `up --build --wait`: API ve web healthy; `/health`, `/ready` ve proxied
  `/api/v1/coverage`: PASS. Canlı kapsama yanıtı toplam 27 şirket/29 kaynak/%50;
  birincil 12 şirket/14 kaynak/%60 ve ikincil 15 şirket/15 kaynak/%40 sonuçlarını
  doğruladı. Kontrol stack'i volume'lar silinmeden kapatıldı.

## Production deploy kanıtı

Kullanıcı ayrı production onayını verdikten sonra davranış revision'ı
`b66c5b2c110c179b2e2052b04fe187bb9ce1b061` yerel Docker + Cloudflare Tunnel
production ortamına deploy edildi.

- Pre-deploy snapshot:
  `internship-tracker-20260811T134756.179922003Z-55d480df7a138026.db`
- Snapshot, candidate API image'ındaki 13 migration ile salt okunur
  `restorecheck` kontrolünden geçti.
- API ve web OCI revision etiketleri exact commit ile eşleşti; iki servis
  healthy, iç `/health` ve `/ready` smoke başarılı oldu.
- Cloudflare dış `/ready` isteği Access yönlendirmesi olan beklenen HTTP `302`
  yanıtını HTTP/2 üzerinden verdi; tünel `TUNNEL_TRANSPORT_PROTOCOL=http2`
  ayarını korudu.
- Kalıcı volume deploy öncesi ve sonrası `internship_tracker_data` kaldı.
  Listing/opportunity/membership/analysis sayıları `8/8/8/8` olarak korundu;
  config uzlaştırması şirket/kaynak sayılarını beklenen `13/15` değerinden
  `27/29` değerine çıkardı. Migration sayısı 13 olarak kaldı.
- `current-local.env` exact Faz 16 revision'ına, `previous-local.env` önceki
  `2ae9f5cf49e31d61f84f08b1437ef76ec455d793` revision'ına atomik geçirildi.
