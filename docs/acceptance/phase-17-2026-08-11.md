# Faz 17 kabul kanıtı — 11 Ağustos 2026

## Araştırma kabulü

- Bilkent CYBERPARK, ODTÜ TEKNOKENT ve Hacettepe Teknokent resmî dizinleri
  yazılım/BT ve geçmiş fırsat sinyali ölçütleriyle tarandı.
- Her park için beş, toplam on beş sonlu aday kaydedildi: 12 `önerilen`, 1
  `düşük_sinyal`, 2 `erişim_manuel`.
- Her aday şirket kimliği, HTTPS resmî domain, teknoloji odağı, teknokent
  kanıtı, fırsat izi, kaynak/erişim sınıfı ve gerekçe taşır.
- On beş adayın hiçbiri production kaynak config'i, scheduler veya bildirim
  akışına eklenmedi.

## Test-first kanıtı

Önce `TestPhase17ResearchCatalogIsFiniteEvidenceBackedAndRuntimeIsolated`
yazıldı. Test, henüz bulunmayan
`docs/research/phase-17-candidates-2026-08-11.json` dosyasını okurken beklenen
`no such file or directory` nedeniyle başarısız oldu. Katalog ve rapor
eklendikten sonra aşağıdaki kabul/regresyon komutu Go 1.26 container'ında geçti:

```bash
go test ./internal/acceptance -run 'TestPhase17|TestPhase16' -count=1 -v
```

Sonuç: Faz 16, Faz 16.5 ve Faz 17 kabul testleri PASS. Faz 17 testi üç zorunlu
parkı, park başına `1..20` sınırını, tüm kanıt URL'lerini, izinli durum/erişim
sözlüklerini, şirket/domain benzersizliğini ve production katalog izolasyonunu
doğruladı.

## Tam kalite

Doğrulanan araştırma revision'ı:
`53695c1e2cff1ed65eaea89987a1efa9f6fde6a8` (upstream `main`).

- İzlenen Go dosyalarında `gofmt -l`: çıktı yok; `git diff --check`: PASS.
- `go test ./... -count=1`, `go test -race ./... -count=1` ve `go vet ./...`:
  PASS.
- API, backup, restorecheck, dbinspect ve vapidkey build'leri: PASS. Araç zinciri
  container'ında repository mount sahipliği VCS damgalamasını engellediği için
  build aynı kaynak üzerinde `-buildvcs=false` ile tamamlandı.
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 açık.
- Node 24.18.0 ile temiz `npm ci`, 5 dosya/19 frontend testi, ayrı typecheck,
  production build ve `npm audit --audit-level=high`: PASS, 0 açık.
- Production contract, exact-commit workflow, deploy runtime ve preflight izin
  sözleşmeleri: PASS.
- Backend ve frontend gerçek Docker image build'leri: PASS.
- Production'dan farklı `internship-tracker-phase17-qa` proje adıyla gerçek
  development Compose `up --build --wait`: API ve web healthy; `/health`,
  `/ready` ve proxied `/api/v1/coverage`: PASS.
- QA coverage mevcut runtime davranışının değişmediğini doğruladı: 27 şirket,
  29 kaynak ve `%52,38` otomatik genel kapsama. Salt okunur `dbinspect`, QA
  volume'unda 27 şirket, 29 kaynak ve 14 migration kaydetti.
- QA container ve network kapatıldı; volume'lar silinmedi. Kalıcı kimlikler
  `internship-tracker-phase17-qa_tracker_data` ve
  `internship-tracker-phase17-qa_tracker_backups` olarak korundu.

Faz 17 araştırma-only olduğu için çalışan uygulama image'larında davranış
değişikliği yoktur ve bu dokümantasyon teslimi için yeni production rollout
gerekmemektedir.
