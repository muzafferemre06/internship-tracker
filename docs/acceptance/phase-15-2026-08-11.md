# Faz 15 kabul kanıtı — 11 Ağustos 2026

## Doğrulanan sözleşme

- Spec'teki on iki birincil şirket production kaynak config'inden gerçek geçici
  SQLite'a kaydedildi; `Commensis` alias'ı kanonik `Commencis` kimliğine çözüldü.
- Kapsama API'si 12 şirket, 14 kaynak, 6 otomatik, 4 manuel ve 4 araştırılıyor
  kaynağı ayrı raporladı. Manuel kaynaklar otomatik kapsama paydasına alınmadı.
- Turkcell GNÇYTNK Staj, sentetik listing olmadan ayrı `closed` program penceresi
  ve resmî başvuru URL'siyle döndü.
- Resmî Commencis Lever fixture'ı ve düşük güvenli Meteksan toplayıcı fixture'ı
  aynı taramada işlendi. İki uygun listing Fırsatlar'a kaydedildi; yalnız
  `official_ats` kaynağı tek Web Push üretti. İkinci tarama yeni listing veya
  ikinci bildirim üretmedi.
- Normal kabul tamamen fixture/fake transport ve geçici SQLite kullanır; canlı
  kariyer sitesi veya ücretli model API çağrısı yapmaz.

## Komut ve sonuç

```text
go test ./internal/acceptance -run TestPhase15 -count=1 -v
=== RUN   TestPhase15PrimaryCoverageTrustAndProgramWindowEndToEnd
--- PASS: TestPhase15PrimaryCoverageTrustAndProgramWindowEndToEnd
PASS
```

## Tam kalite kapısı

Doğrulanan revision: `ba4677d6918029916bdf23ef642bd100a4b0e9be`.

- `go test ./... -count=1`: PASS
- `go test -race ./... -count=1`: PASS
- `go vet ./...`: PASS
- API, backup, restorecheck, dbinspect ve vapidkey build'leri: PASS
- İzlenen Go dosyalarında `gofmt -l`: çıktı yok; `git diff --check`: PASS
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 açık
- Temiz `npm ci`, frontend testleri (5 dosya/17 test), typecheck ve production
  build: PASS
- `npm audit --audit-level=high`: 0 açık
- Production contract, deploy runtime, exact-commit workflow ve preflight izin
  testleri: PASS. Snap Docker'ın `/tmp` namespace ayrımı nedeniyle production
  contract, test değiştirilmeden workspace altındaki geçici dizinle çalıştırıldı.
- Backend ve frontend gerçek Docker image build'leri: PASS
- Gerçek development Compose `up --build --wait`: API ve web healthy;
  `/health`, `/ready` ve proxied `/api/v1/coverage`: PASS. Canlı kapsama yanıtı
  12 şirket, 14 kaynak, %60 otomatik kapsama ve kapalı Turkcell programını
  doğruladı. Kontrol stack'i volume'lar silinmeden kapatıldı.

Production deploy bu kalite kapısından ayrı kullanıcı onayı bekler.
