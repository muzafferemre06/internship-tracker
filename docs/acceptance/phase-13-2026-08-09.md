# Faz 13 kabul kanıtı — 9 Ağustos 2026

Faz 13 kabulü canlı kariyer sitesi veya ücretli AI API kullanmadan tamamlandı.
İki farklı fake kaynak aynı şirket ilanını küçük JSON fixture'dan farklı URL ve
başlık biçimleriyle üretti; fake analizci lokasyon/uygunluk sonucunu verdi. Akış
üretim orchestrator'ı, dokuz migration'lı gerçek geçici SQLite repository,
üretim dashboard HTTP handler'ı, gerçek outbox/dispatcher ve fake Web Push
göndericiyi kullandı.

Çıkış kriteri sonucu:

- Kaynak listing sayısı: 2
- Aktif kanonik fırsat sayısı: 1
- Dashboard kartı: 1 (`opportunity_id` mevcut)
- Bildirim olayı/delivery/push: 1 / 1 / 1
- Bildirim anahtarı: fırsat düzeyli
  `opportunity:<id>:new-primary-suitable:v1`
- Tek taraflı eksik lokasyon kontrolü: 2 ayrı fırsat, otomatik birleşme yok
- Açık lokasyon çelişkisi kontrolü: üyelik split edilir, listing kanıtı silinmez

Kabul komutu:

```bash
go test ./internal/acceptance -run TestPhase13 -count=1 -v
```

Kalite kapıları:

- `go test ./... -count=1`: PASS
- `go vet ./...`: PASS
- `go test -race ./... -count=1`: PASS
- `govulncheck@v1.6.0 ./...`: çağrılan sembollerde 0 güvenlik açığı
- `npm --prefix web test`: 3 dosya / 12 test PASS
- `npm --prefix web run typecheck`: PASS
- `npm --prefix web run build`: production bundle PASS; yerelde eski
  `nobody` sahipli `web/dist` yerine temiz `/tmp` outDir kullanıldı
- `npm --prefix web audit --audit-level=high`: 0 güvenlik açığı
- Deployment shell sözleşmeleri ve production Compose render: PASS
- İzlenen Go dosyalarında `gofmt -l`: çıktı yok
- `go build ./cmd/api`: PASS (çıktı geçici dizine yazıldı)

Normal kabul testleri ağsızdır. Yalnız `npm audit` ve `govulncheck` güncel güvenlik
verisini almak için ağ kullanmıştır; secret, veritabanı veya generated build
çıktısı repoya kaydedilmemiştir.
