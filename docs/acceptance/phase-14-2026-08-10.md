# Faz 14 kabul kanıtı — 10 Ağustos 2026

Faz 14 kabulü canlı kariyer sitesi, LinkedIn isteği veya ücretli AI API çağrısı
yapmadan tamamlandı. Kabul testi üretim `configs/sources.json` dosyasındaki
Havelsan LinkedIn kaydını, on migration'lı gerçek geçici SQLite repository'yi,
üretim orchestrator'ını, deterministik analizciyi, JSON-LD ve robots fixture'ları
ile fake HTTP transportlarını birlikte kullandı.

Çıkış kriteri sonucu:

- Havelsan LinkedIn kaydı: `manual_only`, disabled ve manuel watchlist'te
- LinkedIn HTTP/scraper çağrısı: 0
- İzinli JSON-LD kaynak: 2 ilan, 1 sayfa isteği
- Robots tarafından yasaklanan kardeş kaynak: 0 sayfa isteği, güvenli hata nedeni
- Aynı domain için robots isteği: 1 (cache iki hedef tarafından paylaşıldı)
- Aynı anda domain rezervasyonu: 1
- Anlık ikinci tarama: iki kaynak da `retry_at` ile skip, ek HTTP isteği 0
- İlk/ikinci scan durumu: `partial` / `failed`

Kabul komutu:

```bash
go test ./internal/acceptance -run TestPhase14 -count=1 -v
```

Kalite kapıları:

- `go test ./... -count=1`: PASS
- `go vet ./...`: PASS
- `go test -race ./... -count=1`: PASS
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 güvenlik açığı
- `npm --prefix web test`: 3 dosya / 12 test PASS
- `npm --prefix web run typecheck`: PASS
- `npm --prefix web run build`: production bundle PASS; izinleri eski
  `web/dist` yerine temiz `/tmp` outDir kullanıldı
- `npm --prefix web audit --audit-level=high`: 0 güvenlik açığı
- Deployment exact-commit/runtime/Compose ve preflight sözleşmeleri: PASS
- Backend/frontend container buildleri: PASS (`cacheonly`, image push yok)
- İzlenen Go dosyalarında `gofmt -l`: çıktı yok
- `go build ./cmd/api`: PASS (çıktı geçici dizine yazıldı)

Normal kabul ve birim testleri ağsızdır. Yalnız `npm audit`, `govulncheck` ve
container base/dependency çözümleme adımları güncel dış veriye erişmiştir;
secret, veritabanı veya generated build çıktısı repoya kaydedilmemiştir.
