# Faz 18 ilk batch kabul kanıtı — 11 Ağustos 2026

## Kapsam

- Faz 17'deki 15 şirketin tamamı güncel kimlik, resmî fırsat kaynağı, öğrenci
  sinyali, erişim biçimi ve otomasyon kararıyla ayrıntılı incelendi.
- Kullanıcı onaylı ilk batch `MobileAction`, `SİMSOFT`, `Netaş` ve `Bilişim AŞ`
  olarak production kaynak kataloğuna `secondary` önceliğiyle eklendi.
- MobileAction'ın resmî Lever panosu otomatik izlenir. SİMSOFT formu, Netaş
  COOP sayfası ve Bilişim AŞ staj süreci `manual_only` kalır; form gönderilmez.
- Netaş COOP için tarihli açık pencere kanıtı olmadığından program durumu
  `unknown` olarak tutulur.
- İkincil genel/kıdemli tam zamanlı roller görünür kalır, fakat `staj` veya
  `uzun_donem_staj` türü taşımadan “uygun staj” push'ı üretmez.
- Kapsama, manuel kaynak ve üst durum hata metinleri uzun/boşluksuz içerikte kart
  dışına taşmadan satıra sarılır.

Araştırma revision'ı `03dc03f`; batch uygulaması `b409fa1`; arayüz taşma
düzeltmesi ve kaliteye giren birleşik revision
`eec2f63949f9f0de77830a0ebd62577631bce1c7` olarak upstream `main` üzerindedir.

## Test-first kanıtı

- Lever pano testleri uygulama öncesinde `NewLeverBoardSource` bulunmadığı için
  derlenmedi.
- Bildirim testi, backend odak eşleşmesi bulunan genel tam zamanlı Software
  Engineer rolünün yanlış push ürettiğini göstererek kırmızı oldu.
- Faz 18 kabul testi production config'inde dört şirketi, erişim politikalarını,
  Netaş program penceresini, iki taramalı dedup'ı ve görünür-fakat-sessiz tam
  zamanlı rolleri zorunlu tuttu.
- CSS sözleşme testi uygulama öncesinde kapsam kartında `min-width: 0`, sarılabilir
  metin ve üst durum/manual kart korumaları bulunmadığı için iki beklenen
  assertion ile kırmızı oldu.
- Uygulama sonrasında Faz 18 kabulü, Lever pano birim testleri ve iki CSS
  sözleşme testi PASS oldu.

Normal testler canlı kariyer sitesi veya ücretli model API'si çağırmadı.

## Tam kalite

- İzlenen Go dosyalarında `gofmt -l`: çıktı yok; `git diff --check` ve production
  kaynak JSON parse kontrolü: PASS.
- Go 1.26 container'ında `go test ./... -count=1`,
  `go test -race ./... -count=1`, `go vet ./...`: PASS.
- API, backup, restorecheck, dbinspect ve vapidkey build'leri: PASS.
- `govulncheck@v1.6.0 ./...`: çağrılan/import edilen kodda 0 güvenlik açığı.
- Node 24.18.0 ile temiz `npm ci`, 6 dosya/21 frontend testi, ayrı typecheck,
  production build ve `npm audit --audit-level=high`: PASS, 0 açık.
- Production contract, exact-commit workflow, deploy runtime ve preflight izin
  sözleşmeleri: PASS. Snap Docker'ın sistem `/tmp` görünürlüğü nedeniyle contract
  fixture'ı repository içindeki tek kullanımlık, test sonunda kaldırılan geçici
  dizinde çalıştırıldı.
- Backend ve frontend gerçek Docker image build'leri: PASS.
- Production'dan ayrı `internship-tracker-phase18-qa` proje adıyla gerçek
  development Compose `up --build --wait`: API ve web healthy.
- `/health`, `/ready`, proxied `/api/v1/coverage` ve build edilmiş CSS smoke
  kontrolleri: PASS. CSS bundle'ında `min-width:0`, `overflow-wrap:anywhere` ve
  `white-space:normal` kuralları doğrulandı.
- QA coverage: 31 şirket, 33 kaynak, 12 otomatik, 11 manuel, 10 araştırılıyor;
  genel otomatik kapsam `%54,55`. Birincil 12 şirket/14 kaynak/%60; ikincil 19
  şirket/19 kaynak/%50. Netaş COOP `unknown`.
- Salt okunur `dbinspect`: 31 şirket, 33 kaynak ve 14 migration. QA container ve
  network kaldırıldı; `internship-tracker-phase18-qa_tracker_data` ve
  `internship-tracker-phase18-qa_tracker_backups` volume'ları korundu.

## Production kapısı

Kullanıcının ayrı production onayından sonra doğrulanan
`eec2f63949f9f0de77830a0ebd62577631bce1c7` revision'ı 11 Ağustos 2026'da
deploy edildi.

- Deploy öncesi kalıcı `internship_tracker_data` volume'unda 27 şirket, 29
  kaynak, 31 ilan ve 14 migration doğrulandı.
- `internship-tracker-20260811T190343.475708975Z-cdc37c4c58aafaa8.db`
  snapshot'ı alındı; restore kontrolü hem önceki release hem aday exact revision
  migration setiyle PASS oldu.
- API ve web image'ları doğrudan `eec2f63` Git ağacından üretildi. Compose
  rollout sonunda API, web ve cloudflared healthy/running; API `/health` ve
  `/ready` ile web üzerinden `/ready` PASS oldu.
- Production DB uzlaştırması 31 şirket, 33 kaynak, 31 ilan, 31 fırsat ve 14
  migration gösterdi. İlan/fırsat sayıları korundu; kalıcı data ve backup
  volume'larının 8 Ağustos 2026 oluşturma tarihi ile `internship` proje kimliği
  değişmedi.
- Canlı coverage 12 otomatik kaynak ve 11 benzersiz otomatik şirket gösterdi;
  genel otomatik kapsam `%54,55`, birincil `%60`, ikincil `%50`.
- Cloudflare tunnel aynı sabit digest ile running ve HTTP/2 transport üzerinde.
  Dış `/ready` isteği Cloudflare Access katmanından beklenen HTTP 302/HTTP2
  yanıtını verdi; origin iç readiness ayrıca 200/ready olarak doğrulandı.
- Rollback durumu `current-local=eec2f63`,
  `previous-local=27277f68db9fa5a0e03008faa8d9b1c593bf0ea8` olarak kaydedildi.

İlk snapshot denemesinde yanlışlıkla `internship-tracker` proje adıyla yaratılan
iki boş volume ve boş network, gerçek `internship_tracker_*` production
volume'larından ayrı oldukları doğrulandıktan sonra kaldırıldı; production
verisine dokunulmadı.
