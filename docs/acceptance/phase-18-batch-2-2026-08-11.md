# Faz 18 ikinci batch kabul kanıtı — 11 Ağustos 2026

## Kapsam

- Binalyze, resmî `api.ashbyhq.com/posting-api/job-board/binalyze` public JSON
  panosuyla `ashby_board` / `ats_api` üzerinden otomatik izlenir.
- Insider One'ın güncel `jobs.lever.co/insiderone` panosu kanonik `Insider`
  şirketi altında mevcut `lever_board` adapter'ıyla otomatik izlenir.
- İki kaynak da `official_ats` güvenindedir. Ashby public API ve Lever robots
  erişim bütçeleri en az bir saniye aralık, bir dakika taban cooldown ve bir saat
  üst cooldown uygular.
- Apply sayfaları fetch edilmez; gizli Ashby kayıtları ve başka board/şirket
  bağlantıları kabul edilmez.

## Test-first kanıtı

- İlk Go 1.26 çalıştırması `NewAshbyBoardSource` tanımsız olduğu ve production
  config'inde Insider bulunmadığı için beklendiği şekilde FAIL oldu.
- Uygulama sonrasında Ashby parser ve Batch 2 production config testleri PASS.
- Fixture kabulünde Binalyze ve Insider panoları gerçek SQLite orchestrator'ıyla
  iki kez taranır: ilk taramada dört yeni listing, ikinci taramada sıfır yeni
  listing oluşur.
- Fixture'lardaki genel tam zamanlı teknik roller görünür kalır fakat `staj` veya
  `uzun_donem_staj` türü taşımadıkları için bildirim sayısı sıfırdır.
- Normal testler canlı ATS'lere çağrı yapmaz. Canlı public uç noktalar yalnız
  adapter sözleşmesini doğrulamak için salt okunur incelendi.

Tam repository kalite paketi ve Faz 18'in birleşik kabulü, kalan batch'ler
tamamlandıktan sonra tek exact revision üzerinde çalıştırılacaktır.
